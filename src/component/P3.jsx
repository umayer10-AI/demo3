import React from 'react';

const P3 = () => {
    return (
        <div>
            <button onClick={()=>document.getElementById('my_modal_5').showModal()} className='btn btn-accent'>Click</button>
        </div>
    );
};

export default P3;